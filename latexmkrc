# latexmk configuration for this project
$pdf_mode = 1;
$interaction = 'nonstopmode';
$bibtex_use = 2;
$aux_dir = 'out';
$out_dir = 'out';
# $pdflatex = 'pdflatex -shell-escape %O %S';  # enable if you use minted
